# Sprint 3 구현 계획서: 카카오 로그인 및 프로필 생성

## 📋 목표

비회원으로 성격 테스트를 완료한 사용자가 **카카오 간편 로그인**을 통해 회원으로 전환하고, **프로필을 생성**하여 실제 모임에 참여할 수 있도록 합니다.

---

## 🎯 범위

### 포함 사항
- ✅ 카카오 간편 로그인 (소셜 로그인)
- ✅ 프로필 설정 화면 (이미지/닉네임/자기소개)
- ✅ 프로필 확인 화면
- ✅ 비회원 세션 → 회원 계정 연동

### 제외 사항
- ❌ 이메일/비밀번호 회원가입 (간편 로그인 우선)
- ❌ 프로필 수정 (Sprint 4)
- ❌ 소셜 미디어 연동 (카카오 외)

---

## 📱 화면 플로우

```
#5 모임 추천 TOP 3
    ↓ [프로필 만들고 모임 참여하기]
#6 카카오 로그인
    ↓ [카카오톡으로 간편하게 로그인하기]
카카오 인증 (팝업/리디렉션)
    ↓ 인증 성공
#7 프로필 설정
    ↓ [프로필 이미지 업로드]
    ↓ [닉네임 입력]
    ↓ [자기소개 입력]
    ↓ [완료]
#8 프로필 확인
    ↓ [모임 둘러보기]
홈 화면 (Sprint 4)
```

---

## 🔧 구현 상세

### 1. 카카오 로그인 페이지

#### 파일: `lib/views/pages/auth/kakao_login_page.dart` [NEW]

**화면 #6: 카카오 로그인**

레이아웃:
```
┌─────────────────────────────┐
│                             │
│       온기 로고              │
│                             │
│    환영합니다!               │
│    모임에 참여하려면          │
│    로그인이 필요합니다        │
│                             │
│    [일러스트]                │
│                             │
│  ┌───────────────────────┐ │
│  │  🟡 카카오톡으로       │ │  ← 카카오 브랜드 색상
│  │    간편하게 로그인하기  │ │     (#FEE500)
│  └───────────────────────┘ │
│                             │
│    나중에 할게요             │  ← 텍스트 버튼
│                             │
└─────────────────────────────┘
```

**주요 기능**:
- 카카오 로그인 버튼 (브랜드 가이드라인 준수)
- "나중에 할게요" → 비회원 상태로 홈 화면 진입
- 로그인 성공 시 → 프로필 설정 화면으로 이동

---

### 2. 프로필 설정 페이지

#### 파일: `lib/views/pages/profile/profile_setup_page.dart` [NEW]

**화면 #7: 프로필 설정**

레이아웃:
```
┌─────────────────────────────┐
│ [←]  프로필 설정             │  ← AppBar
├─────────────────────────────┤
│                             │
│     프로필을 완성해주세요     │  ← Title
│                             │
│        ┌───────┐            │
│        │       │            │  ← 프로필 이미지
│        │  📷   │            │     (기본 아바타)
│        │       │            │
│        └───────┘            │
│      [사진 선택하기]          │  ← 텍스트 버튼
│                             │
│  닉네임                      │  ← Label
│  ┌─────────────────────┐   │
│  │ 닉네임을 입력하세요   │   │  ← TextField
│  └─────────────────────┘   │
│  *2-10자, 한글/영문/숫자    │  ← Helper text
│                             │
│  자기소개 (선택)             │  ← Label
│  ┌─────────────────────┐   │
│  │                     │   │  ← Multiline TextField
│  │ 간단히 소개해주세요   │   │     (최대 100자)
│  │                     │   │
│  └─────────────────────┘   │
│  50/100                     │  ← Character counter
│                             │
│  [완료]                     │  ← Primary 버튼
│                             │
└─────────────────────────────┘
```

**주요 기능**:
- 프로필 이미지 선택 (파일 선택 또는 기본 아바타)
- 닉네임 입력 및 유효성 검사
  - 2-10자
  - 중복 확인 (백엔드 API)
  - 한글/영문/숫자만 허용
- 자기소개 입력 (선택, 최대 100자)
- 실시간 글자 수 카운터

---

### 3. 프로필 확인 페이지

#### 파일: `lib/views/pages/profile/profile_confirm_page.dart` [NEW]

**화면 #8: 프로필 확인**

레이아웃:
```
┌─────────────────────────────┐
│        프로필 완료!          │  ← AppBar
├─────────────────────────────┤
│                             │
│  환영합니다, [닉네임]님! 🎉  │  ← Celebration message
│                             │
│        ┌───────┐            │
│        │       │            │  ← 프로필 이미지
│        │  👤   │            │
│        └───────┘            │
│                             │
│       [닉네임]              │  ← Display Large
│                             │
│   "자기소개 내용..."         │  ← Body text
│                             │
│  ────────────────────       │
│                             │
│   성격 유형                  │  ← Section
│   도전적인 탐험가            │
│                             │
│   성격 점수                  │
│   사교성  ████████░░ 85    │  ← 점수 바
│   활동성  ████░░░░░░ 45    │
│   ...                       │
│                             │
│  [모임 둘러보기]             │  ← Primary 버튼
│                             │
└─────────────────────────────┘
```

**주요 기능**:
- 생성된 프로필 정보 표시
- 성격 테스트 결과 요약
- "모임 둘러보기" → 홈 화면 진입

---

## 🔌 백엔드 API 요구사항

### ⚠️ 백엔드 팀 협조 필수

다음 API가 **반드시 구현되어야 합니다**:

#### 1. 카카오 로그인 인증

```http
POST /api/v1/auth/kakao
Content-Type: application/json

Request:
{
  "access_token": "카카오_액세스_토큰"
}

Response:
{
  "success": true,
  "data": {
    "access_token": "JWT_액세스_토큰",
    "refresh_token": "JWT_리프레시_토큰",
    "user": {
      "id": 123,
      "email": "user@kakao.com",
      "name": "홍길동",
      "profile_image": "https://...",
      "is_new_user": true  // 신규 유저 여부
    }
  }
}
```

**백엔드 구현 필요사항**:
- 카카오 OAuth 2.0 연동
- JWT 토큰 발급 시스템
- 사용자 계정 생성/조회

---

#### 2. 프로필 생성/수정

```http
POST /api/v1/users/profile
Authorization: Bearer {access_token}
Content-Type: multipart/form-data

Request:
{
  "nickname": "온기유저",
  "bio": "안녕하세요!",
  "profile_image": File  // 이미지 파일 (선택)
}

Response:
{
  "success": true,
  "data": {
    "id": 123,
    "nickname": "온기유저",
    "bio": "안녕하세요!",
    "profile_image_url": "https://...",
    "created_at": "2024-12-02T00:00:00Z"
  }
}
```

---

#### 3. 닉네임 중복 확인

```http
GET /api/v1/users/check-nickname?nickname=온기유저
Authorization: Bearer {access_token}

Response:
{
  "success": true,
  "data": {
    "available": true  // true: 사용 가능, false: 중복
  }
}
```

---

#### 4. 비회원 세션 연동

```http
POST /api/v1/guest/link
Authorization: Bearer {access_token}
Content-Type: application/json

Request:
{
  "session_id": "비회원_세션_ID"
}

Response:
{
  "success": true,
  "message": "Guest session linked to user account"
}
```

**효과**:
- 비회원 설문 답변 → 회원 프로필로 이동
- UserProfile 생성/업데이트
- 세션 영구 보존

---

## 📦 데이터 모델

### 파일: `lib/models/entities/user_model.dart` [NEW]

```dart
class User {
  final int id;
  final String email;
  final String name;
  final String? profileImageUrl;
  final String? nickname;
  final String? bio;
  
  User({
    required this.id,
    required this.email,
    required this.name,
    this.profileImageUrl,
    this.nickname,
    this.bio,
  });
  
  factory User.fromJson(Map<String, dynamic> json) {
    return User(
      id: json['id'],
      email: json['email'],
      name: json['name'],
      profileImageUrl: json['profile_image_url'],
      nickname: json['nickname'],
      bio: json['bio'],
    );
  }
}
```

### 파일: `lib/models/entities/auth_response_model.dart` [NEW]

```dart
class AuthResponse {
  final String accessToken;
  final String refreshToken;
  final User user;
  final bool isNewUser;
  
  AuthResponse({
    required this.accessToken,
    required this.refreshToken,
    required this.user,
    required this.isNewUser,
  });
  
  factory AuthResponse.fromJson(Map<String, dynamic> json) {
    return AuthResponse(
      accessToken: json['access_token'],
      refreshToken: json['refresh_token'],
      user: User.fromJson(json['user']),
      isNewUser: json['is_new_user'] ?? false,
    );
  }
}
```

---

## 🔧 ViewModel

### 파일: `lib/viewmodels/auth_viewmodel.dart` [NEW]

**책임**:
- 카카오 로그인 처리
- JWT 토큰 관리
- 사용자 인증 상태 관리
- 로그인/로그아웃

**상태**:
```dart
class AuthViewModel extends ChangeNotifier {
  User? currentUser;
  bool isLoggedIn = false;
  bool isLoading = false;
  String? error;
  
  // 메서드
  Future<void> loginWithKakao();
  Future<void> logout();
  Future<void> linkGuestSession(String sessionId);
  Future<bool> checkNicknameAvailability(String nickname);
}
```

---

### 파일: `lib/viewmodels/profile_viewmodel.dart` [NEW]

**책임**:
- 프로필 생성/수정
- 프로필 이미지 업로드
- 닉네임 유효성 검사

**상태**:
```dart
class ProfileViewModel extends ChangeNotifier {
  String? nickname;
  String? bio;
  File? profileImage;
  bool isNicknameValid = false;
  bool isLoading = false;
  
  // 메서드
  Future<void> uploadProfileImage(File image);
  Future<bool> checkNickname(String nickname);
  Future<void> createProfile();
}
```

---

## 🎨 디자인 가이드

### 카카오 로그인 버튼
- 배경색: `#FEE500` (카카오 브랜드 컬러)
- 텍스트: `#000000` (검정)
- 아이콘: 카카오톡 로고
- 높이: 56px
- Border Radius: 12px

참고: [카카오 로그인 브랜드 가이드](https://developers.kakao.com/docs/latest/ko/reference/design-guide)

### 프로필 이미지
- 크기: 120x120px (원형)
- 기본 아바타: Material Icons `account_circle`
- Border: 2px solid Primary color

### 입력 필드
- TextField Border Radius: 8px
- Helper text: Caption 스타일
- Error text: Error color (#DC2626)

---

## 🔄 상태 관리

**Provider 추가**:

```dart
// main.dart 수정
MultiProvider(
  providers: [
    ChangeNotifierProvider(create: (_) => PersonalityTestViewModel()),
    ChangeNotifierProvider(create: (_) => AuthViewModel()),        // NEW
    ChangeNotifierProvider(create: (_) => ProfileViewModel()),     // NEW
  ],
  child: OngiApp(),
)
```

---

## 🧪 Mock 구현 전략

백엔드 준비 전까지 **Mock 인증**으로 화면 구현:

### 파일: `lib/models/api/mock_auth_api.dart` [NEW]

```dart
class MockAuthApi {
  // 카카오 로그인 시뮬레이션
  Future<AuthResponse> loginWithKakao(String accessToken) async {
    await Future.delayed(Duration(seconds: 1));
    
    // Mock 사용자 반환
    return AuthResponse(
      accessToken: 'mock_jwt_token_${DateTime.now().millisecondsSinceEpoch}',
      refreshToken: 'mock_refresh_token',
      user: User(
        id: 1,
        email: 'test@kakao.com',
        name: '테스트유저',
        profileImageUrl: null,
      ),
      isNewUser: true,
    );
  }
  
  // 프로필 생성
  Future<User> createProfile({
    required String nickname,
    String? bio,
    File? profileImage,
  }) async {
    await Future.delayed(Duration(seconds: 1));
    
    return User(
      id: 1,
      email: 'test@kakao.com',
      name: '테스트유저',
      nickname: nickname,
      bio: bio,
      profileImageUrl: profileImage != null 
          ? 'https://via.placeholder.com/120' 
          : null,
    );
  }
  
  // 닉네임 중복 확인
  Future<bool> checkNickname(String nickname) async {
    await Future.delayed(Duration(milliseconds: 500));
    return nickname.length >= 2; // 2자 이상이면 사용 가능
  }
}
```

---

## 📋 체크리스트

### 데이터 모델
- [ ] User 모델
- [ ] AuthResponse 모델
- [ ] Profile 모델

### API (Mock)
- [ ] POST /auth/kakao - 카카오 로그인
- [ ] POST /users/profile - 프로필 생성
- [ ] GET /users/check-nickname - 닉네임 중복 확인
- [ ] POST /guest/link - 비회원 세션 연동

### ViewModel
- [ ] AuthViewModel
- [ ] ProfileViewModel

### UI - 페이지
- [ ] KakaoLoginPage (카카오 로그인)
- [ ] ProfileSetupPage (프로필 설정)
- [ ] ProfileConfirmPage (프로필 확인)

### UI - 위젯
- [ ] KakaoLoginButton (카카오 로그인 버튼)
- [ ] ProfileImagePicker (프로필 이미지 선택)
- [ ] NicknameTextField (닉네임 입력 + 중복 확인)
- [ ] BioTextField (자기소개 입력)

### 네비게이션
- [ ] ClubRecommendation → KakaoLogin
- [ ] KakaoLogin → ProfileSetup
- [ ] ProfileSetup → ProfileConfirm
- [ ] ProfileConfirm → Home (Sprint 4)

### 상태 관리
- [ ] Provider 추가 (Auth, Profile)
- [ ] JWT 토큰 로컬 스토리지 저장

---

## ⚠️ 백엔드 협조 사항

### 우선순위 1: 카카오 인증 시스템
- 카카오 OAuth 2.0 연동
- JWT 토큰 발급
- 사용자 계정 생성/조회

### 우선순위 2: 프로필 API
- 프로필 생성/수정
- 이미지 업로드 (S3 또는 로컬)
- 닉네임 중복 확인

### 우선순위 3: 비회원 연동
- Guest Session → User 연동
- 설문 데이터 이전

---

## 📅 예상 작업 시간

### 프론트엔드 (Mock 기준)
- 데이터 모델: 1시간
- Mock API: 1-2시간
- ViewModel: 2-3시간
- UI 구현: 4-6시간
- 테스트: 1-2시간

**총 예상 시간**: 9-14시간

### 백엔드 연동 (백엔드 준비 후)
- API 팩토리 패턴 적용: 1-2시간
- 실제 API 연동 테스트: 2-3시간

---

## 🎯 성공 기준

1. ✅ 카카오 로그인 버튼 클릭 가능
2. ✅ 로그인 후 프로필 설정 화면 진입
3. ✅ 프로필 이미지/닉네임/자기소개 설정 가능
4. ✅ 닉네임 유효성 검사 작동
5. ✅ 프로필 확인 화면에서 정보 표시
6. ✅ 비회원 설문 결과 회원 프로필로 연동
7. ✅ "모임 둘러보기" 버튼으로 홈 화면 진입

---

## 📚 참고 자료

- [카카오 로그인 가이드](https://developers.kakao.com/docs/latest/ko/kakaologin/common)
- [카카오 디자인 가이드](https://developers.kakao.com/docs/latest/ko/reference/design-guide)
- [Flutter 이미지 업로드](https://pub.dev/packages/image_picker)
- [JWT 토큰 관리](https://pub.dev/packages/flutter_secure_storage)

---

**작성일**: 2024-12-02  
**Sprint**: 3  
**버전**: 1.0  
**상태**: 계획 수립 완료
